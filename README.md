# phoenix_live_view_unrecoverable_form

A phoenix live view with a form that does not recover after crashes or disconnections but it should.

### Run it.

-  `mix phx.server`

- Go to `http://localhost:4000/form`
